import { useState } from 'react'; import { Link, router } from 'expo-router'; import { Text } from 'react-native'; import { useForm, Controller } from 'react-hook-form'; import { z } from 'zod'; import { Screen } from '@/components/Screen'; import { FormField } from '@/components/FormField'; import { PrimaryButton } from '@/components/PrimaryButton'; import { api, ApiError } from '@/lib/api'; import { useAuth } from '@/store/auth';
const schema=z.object({email:z.string().email(),password:z.string().min(1)});type Values=z.infer<typeof schema>;
export default function Login(){const [failure,setFailure]=useState('');const signIn=useAuth(x=>x.signIn);const {control,handleSubmit,formState:{isSubmitting}}=useForm<Values>({defaultValues:{email:'',password:''}});const submit=handleSubmit(async values=>{const parsed=schema.safeParse(values);if(!parsed.success){setFailure('Enter a valid email and password.');return;}try{await signIn(await api<{access_token:string;refresh_token:string}>('/auth/login',{method:'POST',body:JSON.stringify(values)}));router.replace('/');}catch(e){setFailure(e instanceof ApiError?e.message:'Unable to connect.');}});return <Screen>{failure?<Text accessibilityRole="alert" style={{color:'#8A1C1C'}}>{failure}</Text>:null}<Controller control={control} name="email" render={({field})=><FormField label="Email" autoCapitalize="none" keyboardType="email-address" value={field.value} onChangeText={field.onChange}/>}/><Controller control={control} name="password" render={({field})=><FormField label="Password" secureTextEntry value={field.value} onChangeText={field.onChange}/>}/><PrimaryButton label="Log in" onPress={submit} loading={isSubmitting}/><Link href="/(auth)/forgot-password">Forgot your password?</Link></Screen>}
