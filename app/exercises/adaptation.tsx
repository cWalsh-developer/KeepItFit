import { useState } from 'react'; import { Alert, Text } from 'react-native'; import { router, useLocalSearchParams } from 'expo-router'; import { Screen } from '@/components/Screen'; import { FormField } from '@/components/FormField'; import { PrimaryButton } from '@/components/PrimaryButton'; import { api } from '@/lib/api';
export default function NewAdaptation(){const{exerciseId,name}=useLocalSearchParams<{exerciseId:string;name:string}>();const[title,setTitle]=useState('');const[setup,setSetup]=useState('');const[notes,setNotes]=useState('');const[sides,setSides]=useState('');const[tags,setTags]=useState('');async function save(){if(!exerciseId){Alert.alert('Exercise missing');return;}await api('/adaptations',{method:'POST',body:JSON.stringify({exercise_id:exerciseId,title:title||null,setup_instructions:setup,personal_notes:notes,side_notes:sides,tags})});router.back()}return <Screen><Text>Personal adaptation for {name}. This records what works for you and is not medical advice.</Text><FormField label="Adaptation title" value={title} onChangeText={setTitle}/><FormField label="Setup instructions" value={setup} onChangeText={setSetup} multiline/><FormField label="Personal notes" value={notes} onChangeText={setNotes} multiline/><FormField label="Left and right side notes" value={sides} onChangeText={setSides} multiline/><FormField label="Tags (comma separated)" value={tags} onChangeText={setTags}/><PrimaryButton label="Save adaptation" onPress={()=>void save()}/></Screen>}

